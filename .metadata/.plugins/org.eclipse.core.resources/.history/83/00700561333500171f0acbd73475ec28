package step06;
/*
 * 
 */

class Member3 {
   String name;
   int[] scores = new int[3];
   int total;
   float aver;
	
	public void init(String name, int kor, int eng, int math) {
   	this.name = name;
   	m.scores[0] = kor;
   	m.scores[1] = eng;
   	m.scores[2] = math;
   }
   
   public void compute(Member3 m) {
  	 m.total = m.scores[0] + m.scores[1] + m.scores[2];
  	 m.aver = m.total / 3f;
   	
   }
   
   public void print(Member3 m) {
   	System.out.printf("%s, %d, %d, %d, %d, %f\n",
   			m.name, m.scores[0], m.scores[1], m.scores[2], m.total, m.aver);
   }

}