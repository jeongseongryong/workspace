/* 초기화 블록: 생성자 3 
 * => 여러개의 생성자 중에서 호출될 생성자를 지정하기!
 * => 문법
 *    new 클래스명(아규먼트, ...)
 *    => 인스턴스 생성시키는 명령문에서 어떤 값을 넘기느냐에 따라
 *       호출될 생성자가 결정된다.
 * 
 */
package step07;

public class Test03_5 {
  static class Student {
  	 int age = 20;

     Student() {
    	 System.out.println("Student()...호출");
     }
   
  }
  
  public static void main(String[] args) {
  	new Student();
  	System.out.println("----------------------");
  	new Student();
  }
}
