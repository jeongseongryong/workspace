/*사용자 정의 데이터 타입: 1클래스 1파일
 * => 보통 유지보수를 쉽게하기 위해,1 클래스 당 1파일 에 작성한다.
 */
package step02;


public class Test17 {
  
       
			public static void printStudent(Student s /* Student 인스턴스의 주소를 받을 변수*/){ 
				System.out.printf("%s,%d,%d,%d,%d, %.1f\n",
						s.name,
						s.scores[0],
						s.scores[1],
						s.scores[2],
						s.total, s.aver);	
	}

  
	
      for(int i=0; i< arr1.length; i++) {
      	Student.print(arr1[i]);
    }
  }
}
 /* 
  * primitive data type
  * 
  * */ 
